version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "joshi-todo-app-task"
        LoadBalancerInfo:
          ContainerName: "todo-app"
          ContainerPort: 3000
